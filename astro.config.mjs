// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build
export default defineConfig({
  // Static site — builds to ./dist and deploys cleanly to Netlify.
  output: 'static',
});
